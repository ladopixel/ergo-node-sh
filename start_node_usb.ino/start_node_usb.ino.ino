// ┌───────────────────────────┐
// │ Developed by ladopixel    │
// │ Execute Ergo node v5.0.13 │
// └───────────────────────────┘


// I am currently working on this file...


#include <Keyboard.h>

String ERGO;
String url_star_node_sh;

void setup() {
  Keyboard.begin();
}

void loop() {

  delay(2000);
  
  // Keyboard.print(comando_uno);
  // Keyboard.press(KEY_RETURN);
  // delay(1000);
  // Keyboard.releaseAll();
  
  delay(15000);
  
}